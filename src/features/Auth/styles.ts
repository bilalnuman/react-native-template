import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    formWrapper: {
        width: '100%',
        maxWidth: 400,
        marginHorizontal: 'auto',
    },
    linkText: {
        textAlign: 'center',
        fontSize: 16,
        color: "#fff",
        marginTop: 16
    },
    link: {
        color: '#fff',
        fontWeight: 'bold',

    },
});